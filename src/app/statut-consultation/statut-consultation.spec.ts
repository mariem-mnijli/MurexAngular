import { StatutConsultation } from './statut-consultation';

describe('StatutConsultation', () => {
  it('should create an instance', () => {
    expect(new StatutConsultation()).toBeTruthy();
  });
});
